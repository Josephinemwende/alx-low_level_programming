#include <stdio.h>
#include <stdlib.h>

/**
 * main - Entry point
 * Return: Always 0 (Success)
 */
int main(void)
{
	char alphabets;

	for (alphabets = 'z'; alphabets <= 'z'; alphabets--)
		putchar(alphabets);
	putchar('\n');
	return (0);
}
